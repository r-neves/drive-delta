package app.drivedelta.ui.navigation

/**
 * Navigation route constants. Only Auth and Dashboard are implemented in Checkpoint 1; later
 * checkpoints add Cars, Places, History, Tracking, Trip Detail, etc.
 */
object NavDestinations {
    const val AUTH = "auth"
    const val DASHBOARD = "dashboard"
}
