# Declares the locale of the default (unqualified) res/values/ resources so AGP can generate the
# per-app locale config (android:localeConfig). Our default strings are English.
# See https://developer.android.com/r/studio-ui/build/automatic-per-app-languages
unqualifiedResLocale=en-US
