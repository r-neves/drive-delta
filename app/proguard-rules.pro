# Release minification is disabled for the MVP; full rules land in Checkpoint 10.
# Keep this file so the release buildType's proguardFiles reference resolves.

# Kotlin metadata
-keepattributes *Annotation*, InnerClasses, Signature, RuntimeVisible*Annotations
